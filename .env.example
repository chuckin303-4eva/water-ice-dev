# Copy to .env and fill in real values locally. Never commit .env.
# No variables defined yet — add one entry here every time code starts reading
# a new environment variable, with a comment explaining what it's for.
